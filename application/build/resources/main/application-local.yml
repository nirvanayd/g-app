spring:
  config:
    activate:
      on-profile: local # 환경이름설정

  datasource:
    url: 'jdbc:mysql://localhost:3306/soulstation?serverTimezone=Asia/Seoul&characterEncoding=UTF-8'
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver