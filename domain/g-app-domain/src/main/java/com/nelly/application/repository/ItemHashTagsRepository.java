package com.nelly.application.repository;

import com.nelly.application.domain.ItemHashTags;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ItemHashTagsRepository extends JpaRepository<ItemHashTags, Long> {
}
